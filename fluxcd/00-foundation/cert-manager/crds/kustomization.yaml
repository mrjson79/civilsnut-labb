apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Install cert-manager CRDs before the HelmRelease
  # This ensures Certificate, ClusterIssuer, etc. CRDs are available for dry-run validation
  - https://github.com/cert-manager/cert-manager/releases/download/v1.19.2/cert-manager.crds.yaml
