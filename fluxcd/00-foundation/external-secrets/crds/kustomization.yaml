apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Install external-secrets CRDs before the HelmRelease
  # This ensures ExternalSecret, ClusterSecretStore, etc. CRDs are available for dry-run validation
  - https://raw.githubusercontent.com/external-secrets/external-secrets/v1.2.1/deploy/crds/bundle.yaml
