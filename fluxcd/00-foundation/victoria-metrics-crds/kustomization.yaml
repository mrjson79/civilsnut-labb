apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Install VictoriaMetrics operator CRDs before the HelmRelease
  # This ensures VMPodScrape, VMServiceScrape, etc. CRDs are available for dry-run validation
  - https://raw.githubusercontent.com/VictoriaMetrics/operator/refs/heads/master/config/crd/overlay/crd.yaml
