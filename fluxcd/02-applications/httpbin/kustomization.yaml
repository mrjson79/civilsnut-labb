apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  annotations:
    flux.weave.works/depends-on: "shared-gateway"

resources:
  - namespace.yaml
  - service.yaml
  - endpointslice.yaml
  - httproute.yaml
  - referencegrant.yaml
