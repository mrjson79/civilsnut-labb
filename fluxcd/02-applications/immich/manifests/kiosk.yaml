# immich-kiosk: a fullscreen slideshow driven by the Immich API. This is what
# the Home Assistant Family dashboard iframes - HA's own Immich integration
# only exposes sensors, an update entity and a media source, so it can browse
# albums but cannot render a photo frame.
#
# Stateless: no volume, no database. It reads assets from immich-server over
# the cluster network with an API key and streams them to the browser.
#
# The API key ships as a placeholder (see externalsecrets.yaml) - until it is
# replaced in 1Password the pod runs but every request 401s.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: immich-kiosk
  namespace: immich
  labels:
    app: immich-kiosk
spec:
  replicas: 1
  selector:
    matchLabels:
      app: immich-kiosk
  template:
    metadata:
      labels:
        app: immich-kiosk
    spec:
      securityContext:
        runAsNonRoot: true
        # The image's USER is the string "nonroot", and the kubelet cannot
        # verify a non-numeric user against runAsNonRoot - it fails the
        # container with CreateContainerConfigError rather than resolving the
        # name. 65532 is that user's uid/gid from the image's /etc/passwd.
        runAsUser: 65532
        runAsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: kiosk
          image: ghcr.io/damongolding/immich-kiosk:0.42.0
          # distroless, already USER nonroot - so it clears the "restricted"
          # PodSecurity profile even though this namespace only enforces
          # baseline. Nothing in a read-only slideshow needs capabilities.
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: 3000
              protocol: TCP
          env:
            # In-cluster, so plain http and no round trip through the gateway
            - name: KIOSK_IMMICH_URL
              value: http://immich-server.immich.svc.cluster.local:2283
            - name: KIOSK_PORT
              value: "3000"
            # Seconds per photo. 60 is the upstream default and far too slow
            # for something you walk past.
            - name: KIOSK_DURATION
              value: "20"
            - name: KIOSK_SHOW_TIME
              value: "true"
            - name: KIOSK_TIME_FORMAT
              value: "24"
            - name: KIOSK_SHOW_DATE
              value: "true"
            - name: KIOSK_DATE_FORMAT
              value: "YYYY-MM-DD"
            # Unset, kiosk draws random assets from the key owner's OWN library
            # - and frame@civilsnut.se owns nothing, so the frame errored with
            # "no assets found for random". "all" is a kiosk keyword: every
            # album the account can access, owned AND shared. That makes
            # sharing an album to the Frame user the way photos get on the
            # wall. (Other keywords: owned, shared, favorites - or explicit
            # comma-separated album IDs.)
            - name: KIOSK_ALBUMS
              value: "all"
          envFrom:
            - secretRef:
                name: immich-kiosk
          resources:
            requests:
              memory: 64Mi
              cpu: 20m
            limits:
              memory: 256Mi
              cpu: 500m
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: immich-kiosk
  namespace: immich
  labels:
    app: immich-kiosk
spec:
  selector:
    app: immich-kiosk
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
