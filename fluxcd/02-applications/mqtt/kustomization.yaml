apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

metadata:
  annotations:
    # Depends on external-secrets for secrets management
    flux.weave.works/depends-on: "external-secrets"

resources:
  - manifests
