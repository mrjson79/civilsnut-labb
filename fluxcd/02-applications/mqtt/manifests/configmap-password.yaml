# This ConfigMap has been removed - MQTT passwords are now managed via external-secrets
# The password.txt file is now mounted directly from the mqtt-credentials-secret
# Update the deployment to mount the secret instead of this ConfigMap
